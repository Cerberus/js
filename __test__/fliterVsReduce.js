// describe('array', () => {
// 	{
// 		let arr
// 		beforeEach(() => {
// 			arr = [...Array(1000)].map((_, index) => index)
// 		})
//
// 		const updateItem = item => item + ''
//
// 		it('filter + map', () => {
// 			arr.filter(item => item % 2).map(item => updateItem(item)) // ? $.length
// 		})
//
// 		it('reduce', () => {
// 			arr.reduce((acc, item) => {
// 				if (!(item % 2)) return acc
// 				return [...acc, updateItem(item)] // ?.
// 			}, []) // ? $.length
// 		})
// 	}
// })
